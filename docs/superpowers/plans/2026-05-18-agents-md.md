# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a root-level `AGENTS.md` that defines this repository as an AutoHotkey library, requires AutoHotkey v2 syntax, and documents concise project style guidance including One True Brace formatting.

**Architecture:** The change is a single documentation file at the repository root. The file should be short, directive, and organized into sections covering project identity, AHKv2-only language requirements, code style, naming and organization, comments, and change expectations.

**Tech Stack:** Markdown, repository documentation

---

### Task 1: Write The Root AGENTS.md File

**Files:**
- Create: `AGENTS.md`
- Reference: `docs/superpowers/specs/2026-05-18-agents-md-design.md`

- [ ] **Step 1: Create `AGENTS.md` with the approved sections and constraints**

```md
# AGENTS.md

## Project Overview

This repository is a library. Favor reusable APIs, clear module boundaries, and maintainable public-facing behavior over application-specific shortcuts.

## Language Baseline

- Use AutoHotkey v2 syntax and APIs only.
- Do not introduce AutoHotkey v1.x syntax.
- Do not use command-style legacy statements.
- Do not add compatibility shims or mixed v1/v2 examples unless the project explicitly adds that requirement later.

## Code Style

- Use One True Brace (OTB) style.
- Keep formatting and indentation consistent within each file.
- Prefer straightforward, readable code over dense shorthand.
- Keep functions and classes focused on a clear responsibility.

## Naming And Organization

- Use clear, descriptive names for files, functions, classes, variables, and parameters.
- Group related functionality together instead of scattering small pieces across many files.
- Keep public library entry points easy to find.

## Comments And Documentation

- Use comments to explain intent when the code is not self-evident.
- Avoid redundant comments that restate the code.
- Document public behavior and usage expectations where appropriate.

## Change Expectations

- Preserve AHKv2 consistency across the codebase.
- Follow the documented style rules when adding or editing code.
- Avoid introducing patterns that make the library harder to understand, use, or maintain.
```

- [ ] **Step 2: Review the file for spec alignment and concise wording**

Check that the file clearly states all of the following:

- The repository is a library.
- AutoHotkey v2 is required.
- AutoHotkey v1.x syntax is disallowed.
- OTB brace style is explicitly required.
- Naming, organization, and comment guidance are present.

Expected result: all five checks pass with no added workflow or policy sections.

- [ ] **Step 3: Verify the repository state**

Run: `git status --short`
Expected: `AGENTS.md` appears as a new untracked or staged file, and no unintended files were modified by this task.

- [ ] **Step 4: Commit**

```bash
git add AGENTS.md docs/superpowers/specs/2026-05-18-agents-md-design.md docs/superpowers/plans/2026-05-18-agents-md.md
git commit -m "docs: add AGENTS guidance"
```

Expected: a new commit records the spec, plan, and root `AGENTS.md` file.
